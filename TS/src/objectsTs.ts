//Objects in TypeScript
let User: {
    readonly id: number, //id is readonly which means this property cannnot be later changed!
    name: string,
    age: number
    //These are the necesery values That the user object should have.
}
    = { id: 1, name: "Saras", age: 20 }
